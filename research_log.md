# Research log

> 2026-08-24—25 的 49,820 点交互图 SAT 反例、lean CNF、相位归一化
> 与下一轮 CEGIS 记录见 `research_log_20260825.md`。

所有日期为 Asia/Kuala_Lumpur。严格标注三种状态：

- **数值实验**：浮点预筛或求解器模型，不是证书；
- **精确有限证书**：坐标/边/SAT 证明均可机械复核；
- **完整数学证明**：证书与验证器均通过，且论证确实推出目标。

## 2026-07-25：文献与数据基线

### 已核实的原始来源

1. Aubrey de Grey, *The chromatic number of the plane is at least 5*,
   arXiv:1804.02385v3。给出 1581 顶点不可 4 染色单位距离图。
2. Marijn J. H. Heule, *Computing Small Unit-Distance Graphs with
   Chromatic Number 5*, arXiv:1805.12181；以及 *Trimming Graphs Using
   Clausal Proof Optimization*, arXiv:1907.00929v2。后者报告 529 顶点、
   2670 边、约 100000 CPU 小时的缩减图；关键坐标域为
   \(\mathbb Q(\sqrt3,\sqrt{11})\)。
   Jaan Parts 后续的 *Graph minimization, focusing on the example of
   5-chromatic unit-distance graphs in the plane*, arXiv:2010.12665，
   把顶点记录降到 509 点、2442 边。首轮仍选 Heule 529，是因为作者
   仓库把精确坐标、边、CNF 和 DRAT 放在同一可固定版本的数据源中。
3. 作者仓库 `marijnheule/CNP-SAT`，固定提交
   `bb414955a6ef5f49f7df2b245b1e778aa67c068a`。已取得 529 点精确
   Mathematica 坐标、完整边表、4 色 CNF 与 DRAT。
4. Ákos Dúcz, *A note on geometric colorings of the Moser lattice*,
   arXiv:2606.12325v1。它给出整个 Moser lattice 的几何整数 4 染色，
   且染色延伸到 Moser ring。因此把搜索限制在该环内不可能得到
   不可 4 染色子图，更不可能得到不可 5 染色子图。
5. Ákos Dúcz and Dániel Varga, *A unit-distance graph in the plane with
   independence ratio below 1/4*, arXiv:2606.28157。作者在 27 点 Moser
   构形外加入两个极稀有点，使几何分数色数严格大于 4；论文明确使用
   “候选点阻塞最优着色”的筛选思想。
6. Dúcz–Varga 正式补充材料，下载为 `third_party/snail.zip`，
   SHA-256
   `ff6c9fc9df606ee15be9b4e3d14e7144e3d829fbd7fbeca3bc21bdd50e43667c`。
   含 29 个精确 SymPy 复坐标、符号邻接重建和有理 LP 对偶证书。
7. H. Costa Silva Filho, *Exact certification of the coordinate fields
   of the triangle-free Exoo–Ismailescu unit-distance graphs EI17 and EI19*,
   arXiv:2607.19995（2026-07-24 版本）。它展示了低阶图也可能需要
   12 次可解扩张或 20 次、Galois 群 \(S_{20}\) 的不可根式扩张。
   对本项目的工程含义：最终坐标格式不能永远假定“根式字符串足够”。

### 状态结论

检索到的上述原始论文都没有给出不可 5 染色的有限平面单位距离图；
当前下界仍为 5，上界仍为 7。arXiv:2606.12325 排除了仅在 Moser
lattice/ring 内搜索的路线。arXiv:2606.28157 的两个外域点针对几何
分数着色而非普通 5 染色，不能被误报为下界 6。

### 本轮固定输入

- 基图：Heule 529 顶点图；
- 基域：\(\mathbb Q(\sqrt3,\sqrt{11})\)；
- SAT：python-sat 1.9.dev7 的增量求解器；
- 证明复核：drat-trim 提交
  `2e3b2dc0ecf938addbd779d42877b6ed69d9a985`；
- 随机种子：`20260725`；
- 第一候选域：所有现有点对的两个单位圆交点，经浮点 KD-tree 预筛，
  只有通过符号平方距离检查的候选被保留；
- 后续候选域：Dúcz–Varga 外域点所示二次扩张、非单位半径圆交点、
  新旋转/反射，以及以最小多项式＋隔离区间记录的低次扩张。

### 失败分支登记

- `git init`：工作区 `.git` 位置由宿主设为只读，返回
  `Operation not permitted`；不影响研究文件和第三方提交哈希记录。
- 项目初始环境没有 SAT 命令行求解器、`drat-trim` 或 `python-sat`；
  已在项目内安装 python-sat，并从作者仓库构建 drat-trim。
- 作者仓库的 `529-4-sbp.drat` 不能直接用普通 `529-4.cnf` 验证：
  第 30758 行 RAT 失败。与同库 517/553/610/633/803/826/874 数据逐项
  对照后确认缺失的是 `529-4-sbp.cnf`；只加入所有同类实例共有的三条
  对称破除单位子句 `1`, `6`, `23` 后，原始证明由未修改的
  `drat-trim` 验证通过。派生文件和哈希见 `data/heule529/README.md`。

## 实验表

| 时间 | 实验 | 种子 | 输入 | 状态 | 结果 |
|---|---|---:|---|---|---|
| 2026-07-25 | Heule 529 几何重建 | — | 作者 `.vtx/.edge` | 精确证书 | 529 点、2670 边、完全一致 |
| 2026-07-25 | Heule 529 不可 4 染色 DRAT | — | 派生 SBP CNF/作者 DRAT | 精确证书 | `s VERIFIED` |
| 2026-07-25 | 5 色模型枚举 | 20260725 | Heule 529 | SAT 实验 | 初始 128，CEGIS 后共 195 |
| 2026-07-25 | 单位圆交点 CEGIS | 20260725 | Heule 529 | 精确候选＋SAT 实验 | 67 个新反例后池停滞，未得下界 6 |
| 2026-07-25 | 度 3 联合构形 CEGIS | 20260725 | 首轮模型 194 | 精确候选＋SAT 实验 | 再得 67 个反例；1061 点全池仍可 5 染 |
| 2026-07-25 | 第二层圆交点闭包 | 20260725 | 精确 1061 点图 | 精确完整失败证书 | 2510 点、13578 边仍可 5 染 |
| 2026-07-31 | 11 副本完整交互重建 | — | primary-D4 8329 点 | 精确完整失败证书 | 55 对均代数认证；53332 边，仍可 5 染 |
| 2026-08-03 | D4 单点阻塞 CEGIS | — | 完整 8329 点图 | 精确 SAT 失败证书 | 完整边版 10229 点/71130 边仍可 5 染；列边版单点池于 11316 点重复 |
| 2026-08-04 | D4 强制点对 CEGIS | — | 11316 点检查点 | 精确列边 SAT 实验 | 首轮 1057 对；12115 点/81076 边仍可 5 染 |

## 2026-07-25：首轮结果

### 已复现的精确有限证书

- Heule 529 作者坐标：529 个不同精确点；
- 从坐标全点对符号重建：恰好 2670 条单位边，与作者边表完全一致；
- 本地 SAT：4 色 `UNSAT`、5 色 `SAT`；
- 作者 DRAT 的配套 SBP CNF 漏件按同库统一规则重建后，
  `drat-trim` 报告 `s VERIFIED`，30835/30836 个引理进入核心，
  3154242 次消解；
- 这是已知 5 色单位距离图的精确有限证书，不是下界 6。

### CEGIS 输入与计数

- 初始规范 5 染色：128 个；颜色首次出现顺序消除全部颜色置换；
- 中心距离不超过 2 的点对：94148；
- 原始单位圆交点记录：187846；
- 排除基图点并数值去重：151971；
- 数值度至少 4：686；
- 精确恢复且至少 4 个符号单位邻居：248
  （度 4/5/6/7 分别为 137/62/33/16）；
- 候选间符号单位边：151；
- 运行上限：100 轮；实际在第 67 个新反例后停滞；
- 已知规范 5 染色总数：195；
- 最后成功增广：604 点、3130 边、显式 5 染色；
- 累计选择候选：75。

### 覆盖最高的点与点对

在最终 195 个反例模型上，最高单点覆盖为 133/195：

\[
x=\frac{21-3\sqrt5-\sqrt{165}+7\sqrt{33}}{32},\qquad
y=\frac{3\sqrt{15}+7\sqrt3+3\sqrt{55}+7\sqrt{11}}{32}.
\]

其 0 基单位邻居为
`[439, 454, 469, 474, 497, 511]`。

最高相邻候选对是候选 12 与 47，总覆盖 138/195，其中 130 个模型
只有依靠两候选之间的单位边才联合阻塞：

\[
p=\left(\frac{17-\sqrt{33}}{12},
         \frac{\sqrt3+\sqrt{11}}{12}\right),\quad
q=\left(\frac{17+\sqrt{33}}{12},
         \frac{-\sqrt3+\sqrt{11}}{12}\right).
\]

### 停滞与失败证书

单点贪心集合需要 28 点，仍漏掉模型
`[177,189,192,193,194]`；四个相邻候选对
`[14,224]`, `[19,148]`, `[95,109]`, `[96,97]` 补掉前四个，但
模型 194 无法由任何单点或相邻点对阻塞。进一步对全部 248 候选做
完整 list-coloring SAT，模型 194 可扩展。

将 529 点基图与全部候选合并，独立符号验证得到：

- 777 个不同顶点；
- 3989 条完整单位距离边；
- 缺边 0、伪边 0；
- 显式 5 染色的色类大小 `[171,175,159,145,127]`；
- 3989 条边上的同色冲突 0。

所以数值预筛列出的 248 点候选池已被严格排除；这不构成“所有理论
单位圆交点均已完备枚举”的证明。下一候选域必须加入 Moser ring 外的
新旋转、非单位半径圆交点或低次代数扩张。

工件：

- `artifacts/cegis/heule529-unit-circles.json`：
  SHA-256 `cd5a89143a2ddda0e9e1248d01f2044d0dccf06622678b2d90b03565c9965360`；
- `artifacts/cegis/heule529-unit-circles-full-pool.graph.json`：
  SHA-256 `29860a0b911def79f966de47635e9e9bbcb402f1a0c69cee8039eb8557cc833d`；
- 529 作者坐标：
  SHA-256 `ce0cf260e431972c1222521f1b552bc94e7c42040ddf4b4110cee0eaab518dbb`；
- 529 作者边表：
  SHA-256 `4c7e01105d880e2ca84804f73318dabaaffd61b8f826923c30de5c06fdcdf1c5`；
- 作者 DRAT：
  SHA-256 `fc2edf1fb573de69f5f2c786d62c50b7da13e7b2b8bc1b1bb5859bea58ee4cb6`。

### 状态分类

- 文献现状：已核实，仍为 \(\{5,6,7\}\)；
- Heule 529：精确有限证书已复现；
- 777 点全候选池：精确图＋显式 5 染色失败证书；
- 下界 6：**未证明**，没有不可 5 染色 CNF/DRAT。

## 2026-07-25：度 3 联合构形续跑

### 设计与输入

首轮只显式覆盖单点和相邻点对，遗漏了度 3 点通过候选间单位边形成的
更大列表染色阻塞器。第二阶段使用通用列表染色 SAT：对固定基图
5-染色，完整求解全部候选点的允许颜色与候选间异色约束；若 UNSAT，
逐点删除得到删除极小核。

- 数值度至少 3 的种子：1314；
- 精确恢复候选：532；
- 精确度分布 3/4/5/6/7：284/137/62/33/16；
- 候选间精确单位边：707；
- 首轮已选候选按精确坐标映射到新池：75；
- 起始反例：首轮模型 194。

### 首个三点联合阻塞器

首轮模型 194 对 532 点全池的列表染色为 UNSAT。删除极小核为候选
`[98,236,504]`，只有两条候选间单位边
`(98,504)`、`(236,504)`：

\[
v_{98}=\left(\frac{7+\sqrt{33}}{12},
              \frac{-\sqrt{11}+7\sqrt3}{12}\right),
\]
\[
v_{236}=\left(\frac{5+\sqrt{33}}6,
               \frac{\sqrt3+\sqrt{11}}6\right),
\quad
v_{504}=\left(\frac{13+\sqrt{33}}{12},
               \frac{-\sqrt{11}+\sqrt3}{12}\right).
\]

在该固定染色下，端点允许颜色分别为 `{4}`、`{3}`，中心允许
`{3,4}`，两条单位边恰好排除中心的两个颜色。三点中已有一点在首轮
75 点集合内，因此第一次增广新增两点，得到 606 点、3140 边图并找到
新 5-染色。

### CEGIS 终止

- `SAT_COUNTEREXAMPLE`：67 轮；
- 最终全池延拓：第 68 轮；
- 阻塞核大小分布：
  `1:16, 2:17, 3:14, 4:8, 5:5, 6:1, 7:2, 9:3, 15:1`；
- 核平均大小：约 3.149；
- 最后成功增广图：529+199=728 点、3808 条当前精确边，仍可 5 染；
- 随后的基图染色可扩展到全部 532 候选。

对 529+532 点重新做全点对代数重建，而不是复用数值预筛边表：

- 1061 个不同顶点；
- 5397 条完整单位距离边；
- 缺边 0、伪边 0；
- 显式 5 染色色类大小 `[253,252,218,186,152]`；
- 5397 条边同色冲突 0。

因此这是当前度至少 3 圆交点池的**精确有限失败证书**。它不排除数值
预筛漏掉的理论交点、度 2 候选构成的高交互结构、Snail 外域嵌入、新
旋转/反射或更高代数扩张。

运行：

```bash
.venv/bin/python configuration_cegis.py --iterations 100 --min-degree 3 \
  --output artifacts/cegis/heule529-degree3-configurations.json
.venv/bin/python graph_verifier.py --graph-json \
  artifacts/cegis/heule529-degree3-configurations-full-pool.graph.json
```

工件：

- 阶段二报告：
  SHA-256 `6178aa89df3bde573aff8457a14eea9395bda3b76110de39fd627c209cdd35f1`；
- 1061 点完整图：
  SHA-256 `0fcbecb2345ec6d6ffc01ad3cb42dbd69f7f4b30519a6da3a686194f68eb67ec`。

### 当前状态

- 1061 点图：精确坐标、完整边和显式 5-染色均独立通过；
- 下界 6：**仍未证明**；
- 下一候选域：把作者 Snail/G29 作为有内部单位边的整体构形，通过
  精确边对齐嵌入 Heule 529，而不是继续只加单个圆交点。

## 2026-07-25：第二层圆交点闭包

### Snail 路线审计

作者补充材料中的 G29 普通图有 29 点、51 边，本地 SAT 为
3-可染。唯一包含
\(\sqrt{415/8+79\sqrt{33}/8}\) 的新扩张点在普通图中度为 1。
因此论文的强度来自等距独立集约束和分数着色，而不是单个 G29 对普通
5-染色的直接约束。第二层圆交点闭包被选为更直接的下一候选域；
Snail 的异常方向保留为定向旋转种子。

### 第二层联合阻塞探索

以已验证 1061 点图作圆心，最初使用 10 位小数键预筛得到：

- 320457 对可交圆心；
- 639720 个交点记录；
- 548505 个数值外部交点；
- 3321 个数值度至少 3 种子；
- 1428 个精确恢复候选；
- 2402 条候选间精确单位边。

阶段二最终染色被一个五点路径核阻塞。核的允许色列表沿路径为
`{3}—{1,3}—{0,1}—{0,4}—{4}`，所以不可列表染色。检查点式
CEGIS 连续运行 200 轮，选择 480 候选，每轮增广图仍返回 5-染色。
随后直接求解全部 1428 候选列举图，仅 0.32 秒即得到 5-染色。

这些 200 轮是有用的数值/精确边探索，但下述审计发现邻接并不完整，
所以旧工件不作为完整图证书。

### 数值去重缺陷与修复

对旧 2489 点列举图运行独立全点对验证，发现完整图有 13404 边，
列举图只有 13250 边，漏 154 条；全部漏边位于 1061 点基图与第二层
候选之间。

根因是两个精确不同、数值极接近的圆交点会落入同一个 10 位舍入键。
恢复层随后把一个点的生成记录/邻居提示用于另一个点。该问题不是距离
容差过紧：抽查漏边的双精度平方距离误差只有
\(1.1\times10^{-16}\)，精确平方距离为 1。

修复：

1. 数值分桶从 10 位提高到 14 位，只继续作为预筛；
2. 恢复后以精确 `Point` 等式去重并合并生成记录；
3. 把全部基点和保留候选放入共同代数数域，扫描完整二分单位边，
   覆盖数值邻居提示；
4. 最终对所有点重新生成完整单位距离图并直接求色。

新增回归测试分别覆盖完整二分边扫描、不完整邻居提示的重认证以及小图
全闭包直解。

### 修正后的精确失败证书

重新生成得到：

- 数值外部交点：548898；
- 数值度至少 3 种子：3714；
- 精确候选：1449；
- 候选间精确单位边：2452；
- 完整图：1061+1449=2510 点、13578 边；
- 显式 5 染色色类：`[578,604,503,450,375]`。

搜索模块先重建完整图并得到 5-染色；独立 `graph_verifier.py` 再从
2510 个精确坐标重算，结果为：

- 重复顶点 0；
- 完整单位边 13578；
- 缺边 0、伪边 0；
- 全部 13578 边同色冲突 0。

工件：

- `artifacts/cegis/heule1061-second-closure-recertified.json`：
  SHA-256 `dcd6f2add71b12a289a824b1f6e114bbd3c46b49d4341db9315c02975f7bcae0`；
- `artifacts/cegis/heule1061-second-closure-recertified-complete.graph.json`：
  SHA-256 `f64cbfb7fd8ea17637efb465e51f5361f293736e98d2e1ef06e9070ef37f8ced`。

运行：

```bash
.venv/bin/python closure_cegis.py --direct-audit --min-degree 3 \
  --output artifacts/cegis/heule1061-second-closure-recertified.json
.venv/bin/python graph_verifier.py --graph-json \
  artifacts/cegis/heule1061-second-closure-recertified-complete.graph.json
```

### 状态

- 第二层圆交点闭包：**精确完整失败证书**；
- 下界 6：**未证明**；
- 下一搜索必须离开“仅反复取已有点单位圆交点且度至少 3”的单调闭包，
  转向度 2 高交互构形、Snail 异常方向的成组旋转副本，或非单位半径
  圆/低次多项式约束产生的点。

## 2026-07-30：二邻居平移 Heule-529 副本

### 最新文献状态复核

检索窗口延伸至 2026-07-30，只采用 arXiv 原文页和作者数据仓库。没有
发现给出有限不可 5 染单位距离图及可检查证书的新论文，公开普通色数界
仍为 `{5,6,7}`。两篇 2026 年相关原文的边界是：

- Ákos Dúcz, arXiv:2606.12325：整个 Moser lattice 以及 Moser ring
  存在 geometric 4-coloring，因此不再把搜索限制在该环；
- Ákos Dúcz、Dániel Varga, arXiv:2606.28157：构造独立率小于 `1/4`
  的有限单位距离图，从而推出平面的**分数**色数严格大于 4；该结论
  不是普通色数至少 6。

### Snail 异常方向审计

从作者 Snail 补充材料的 1--4 号点恢复异常单位方向
`(-0.9957972522320608, 0.09158511040053083)`，并生成其共轭及六次旋转
的 12 个方向。实测：

- 绕原点旋转一个 Heule-529 副本：528 个新点，只有 36--37 条跨边，
  每个新点至多看见一个基图颜色；
- 沿异常单位方向平移：529 条逐点跨边，每个新点仍至多看见一个基图
  颜色；
- 两类副本都可延拓当前五染色。

该方向保留为组合构形种子，但单副本路线暂停。

### 二邻居平移构形

选择基图第 98 个精确点

\[
r=((\sqrt{33}-5)/12,(\sqrt3+\sqrt{11})/12)
\]

并令

\[
h^2=(25+3\sqrt{33})/8,\qquad
t=(r_x/2+r_yh,\ r_y/2-r_xh).
\]

`h` 的最小多项式是 `8*x^4-50*x^2+41`，Galois 群为非交换
`D4`；它不属于交换多二次域
`Q(sqrt(3),sqrt(5),sqrt(11))`。因此对源点 `p` 和基点 `b`，
`|b-(p+t)|^2=1` 严格等价于 `b=p` 或 `b=p+r`。这给出无需浮点
截断的完备跨边枚举。

第一个副本：

- 529 个新点，与 2510 点基图重合 0；
- 772 条跨边，其中 243 个新点有两个基图邻居；
- 固定旧基图染色时列表着色 UNSAT；
- 不固定旧颜色的 3039 点、17020 边图仍 SAT，求解 2.77 秒。

这完成了一次精确的
`染色 → 阻塞副本 → 增广图 → 新染色` 循环。

### 24 候选 primary-D4 CEGIS

候选取作者 529 点中平方长度为
`1/2 ± sqrt(33)/18` 的 24 个 `r`。运行命令：

```bash
.venv/bin/python translated_copy_cegis.py --primary-d4-cegis \
  --output-graph artifacts/cegis/heule2510-primary-d4-cegis.graph.json \
  --output-report artifacts/cegis/heule2510-primary-d4-cegis.json
```

确定性候选排序下，连续选择的源点零基下标为：

`[201,101,216,97,104,135,184,118,178,206,124]`。

11 轮后其余 13 个候选均可延拓最终模型。结果：

- 8329 个互异精确代数坐标；
- 51378 条已列明精确单位边；
- 显式 5-染色，全部已列明边冲突 0；
- 总运行 395.47 秒；
- 各轮顶点重合合并数均为 0。

独立 `translated_copy_verifier.py` 不导入搜索模块，从 2510 点基图、
作者 529 点模板、上述 11 个 `r` 重新构造每个 `t`、每个坐标和全部
已列明边；验证耗时 79.36 秒，结果：

- 坐标重建一致；
- 重复顶点 0；
- 缺少已列明边 0、意外已列明边 0；
- 保存染色冲突 0；
- 每个平移的非交换 Galois 证书和两个锚点距离均通过。

SHA-256：

- 图：
  `14ef0d43dd4a28baa8587153182a84fa45a4e133411d1f37127dd5060915348b`；
- 报告：
  `c255832dfb16fa6a7dbd15b034eefc907038eb028508391be97b093c5ff57964`。

严格限定：搜索图故意未列举**不同平移副本之间**可能存在的额外单位边。
已列明边子图的 SAT 染色是精确失败证书，但不是全部 8329 点的完整单位
距离图染色。下一步应对 11 个副本间做成对二次扩张交互扫描；任何新增
跨副本边只会加强图，可能使当前染色失效。

### 状态

- primary-D4 平移池：**精确已列明边失败证书**；
- 下界 6：**未证明**；
- 当前最高优先级：完整重建 55 对副本之间的单位边，并在共同代数扩张
  中先用可证明误差界预筛、再逐候选符号认证；随后重跑五染色 SAT。

### 跨副本边：第一轮

对 11 个副本的 55 对做双精度预筛（仅用于找候选）：

- 1954 个平方距离在 `1e-9` 内接近 1；
- 只出现在三对副本 `(0,6)`、`(1,10)`、`(3,7)`；
- 数值计数分别为 648、655、651；
- 当前保存染色有 458 条近单位边同色；
- 所有未保留数值对离 1 的最小间隙约 `2.6743e-6`。这个间隙尚未
  配上有理区间误差界，所以不能单独证明其余 52 对无边。

三对候选随后恢复为精确定理。若两副本使用同一个 `h`，令
`s=r_b-r_a`，则

`t_b-t_a = s/2 + (s_y,-s_x)h`。

这三对均严格满足 `|t_b-t_a|^2=1`。把跨边方程按 `1,h` 分离可得
`p_b=p_a` 或 `p_b=p_a-s`，故边数精确为：

- `(0,6)`：`529+119=648`；
- `(1,10)`：`529+126=655`；
- `(3,7)`：`529+122=651`。

新增回归测试从精确坐标重建三对全部 1954 条边并逐条验证平方距离为 1。

SAT 探索：

1. 在原 51378 边模型的同一增量 CaDiCaL 会话中加入第一对 648 边，
   58.26 秒得到新 5-染色；相对旧染色改变 3253 个顶点，色类大小
   `[1736,1471,1636,1787,1699]`。
2. 再加入第二对 655 边后运行 10 分钟未决，手动终止；状态严格记为
   `UNKNOWN/TIMEOUT`，不是 UNSAT。
3. 同时冷启动的 53332 边三对实例以及固定三角形/不同 CaDiCaL 版本
   均未在数分钟内返回，均已终止且不作为证书。

因此第一对已被精确 SAT 反例排除；前两对联合以及三对联合仍是强候选，
但当前没有 SAT 模型也没有 UNSAT/DRAT 证书。下一步是把 52026、
52681、53332 边三个实例分别落盘为 CNF，以可中断的外部证明型求解器
运行，并为其余 52 对补上严格代数区间排除。

## 2026-08-04：完整 D4 图、单点池与相邻强制点对

### 55 对副本的完备精确重建

后续用共同代数基替代数值间隙推断，逐一认证 11 个平移副本的 55 对：

- 同一二次扩张的副本对按平行线不变量分组，再在基域中精确判等；
- 不同扩张的副本对用 `h0*h1` 系数分离，退化正交情形另验非零残差；
- 仅 `(0,6)`、`(1,10)`、`(3,7)` 三对有边，合计 1954 条；
- `copy_interaction_verifier.py` 不导入搜索实现，重建全部 8329 个坐标和
  53332 条单位边，55 对证书全部 `complete=True`，缺边 0、伪边 0；
- CaDiCaL 195 返回 5-染色，独立逐边验证通过，色类大小
  `[1534,1625,1684,1806,1680]`。

工件 `artifacts/interactions/heule8329-interactions-stage3.graph.json` 的
SHA-256 为
`b34a6e3644dce02d58fdbc6e9933d95b82ec7653529a90fc3557b29f62b376ff`；
对应边文件为
`5d5d4466e11deaee3bb71139a82a23a513040b70640491c0c5c5b4fa45806a25`。
这是完整单位距离图的精确失败证书，不是下界 6。

### 五色单点阻塞 CEGIS

`d4_exact.py` 用基
`sqrt(3)^a sqrt(5)^b sqrt(11)^c h0^d h1^e`（32 个基向量）的有理
系数向量做加乘和齐次单位距离判定。数值颜色分区只定位候选；每个保留
点均由两条垂直平分线恢复为 `(Nx/D,Ny/D)`，五条单位边逐项在该代数中
判为恒等式。

完整边模式连续加入 1900 个阻塞点，得到：

- 10229 点、71130 条完整认证边；
- 最大候选—基图单位度 24，平均约 8.143；
- 最大候选—候选交互度 11，平均约 1.225；
- 整图仍有验证通过的 5-染色。

随后采用声称安全的列边子图模式：每个阻塞点只列出阻塞当轮反例所需
的五条精确单位边。若该子图 UNSAT，则补全边后的图必然仍 UNSAT；但
SAT 结果只排除当前列边构形。单点候选池最终在 2987 个新点后重复：
`heule11316-d4-blockers.json`，11316 点、76565 边，仍有合法 5-染色。

### 相邻四颜色强制点对

对每个缺失颜色 `c`，先找同时看到其余四种颜色的精确候选；两个这样的
候选若严格相距 1，则二者都只能取 `c`，内部单位边使它们联合不可着色。
在 11316 点检查点的 8329 点基图染色限制上得到：

| 缺失色 | 精确强制点 | 精确单位点对 |
|---:|---:|---:|
| 0 | 834 | 177 |
| 1 | 1403 | 214 |
| 2 | 1099 | 230 |
| 3 | 1441 | 234 |
| 4 | 1504 | 202 |

合计 1057 对；坐标复用后新增 799 点、4511 边，其中 1028 条为此前未列
的候选对内部边。12115 点、81076 边实例仍 SAT，求解 92.34 秒。状态
严格记为精确列边 SAT 反例。

继续三轮的批次统计为：

| 轮次 | 可用点对 | 新点 | 新边 | 新内部边 | SAT 时间 |
|---:|---:|---:|---:|---:|---:|
| 2 | 1442 | 939 | 5460 | 1246 | 386.13 s |
| 3 | 1309 | 820 | 4851 | 1090 | 32.05 s |
| 4 | 1008 | 547 | 3409 | 803 | 84.91 s |

最新点对检查点为 14421 点、94796 边、6092 个候选，仍有逐边验证的
5-染色；三轮续跑总耗时 916.95 秒。

### 投影闭包扩展

旧生成器只让原始 8329 点作单位圆心。新增
`D4ProjectiveCoordinateBackend` 对分式中心先清除分母，再用 Cramer
法则恢复候选；四个生成距离、第五颜色距离以及候选间距离均只比较 32
维有理系数。`d4_projective_closure_cegis.py` 因而允许所有已认证候选
作为下一层圆心。小型分式中心回归测试通过后，对 14421 点运行首轮：

- KD-tree 半径 2 预筛圆心对 49174936 个；
- 两个颜色分区的不同交点分别为 6840665、7575438 个；
- 公共交点 8234，排除已有点后 4447，第五色数值命中 1366；
- 首批 25 个候选逐个精确恢复，新增 125 条列示单位边；
- 14446 点、94921 边图仍 SAT，求解 0.65 秒。

这里只把浮点用于定位 1366 个待验候选；被加入的25点及125边均有32维
有理系数恒等式证书。

随后五个每轮100点的批次达到14946点、97421边，均仍 SAT。改为每轮
接收全部可认证阻塞点后，五轮依次加入1268、1400、1591、1846、2000
点；得到23051点、137946边和合法5染色。

第五轮出现 `np.int64` 量化溢出警告。系统性复核定位到37个根式商：
它们的17位 `evalf` 因严重消去返回了伪造的二次幂大坐标。例如一个真实
坐标约为 `(0.5268247552,0.6841340779)` 的点被评为
`(-1073741824,68719476736)`。这不会让非单位边通过32维精确判定，但会
使数值预筛漏点或漏掉重复点。

修复包括：

- `Point.approximate` 使用 `maxn=100000` 的自适应保护精度；
- 近圆心枚举改为颜色对之间的稀疏 KD-tree 查询，不再物化全部近点对；
- 新增 `d4_listed_graph_verifier.py`，不导入任何搜索器，重放全部齐次
  坐标并逐条检查列边、保存染色和精确重复点。

重放23051点工件的结果：84614条非基图边全部严格为单位距离，保存的
5染色合法，色类 `[4431,4680,4477,4732,4731]`；但发现5对精确重复
顶点。因此旧23051点工件不再作为互异点集检查点。`d4_merge_duplicates.py`
将五个等价类的全部邻接约束取并，得到23046点、137927边（合并19边），
重新求解后仍 SAT。后续搜索只从该商图恢复。

齐次规范化后又连续加入三批各2000点，最近三轮精确扫描时间为
215.47、227.70、90.55秒；最后一轮速度提升来自每次恢复后约去96个
齐次系数的共同有理尺度。当前稀疏检查点为29046点、167927边，仍SAT。

对这29046点做候选交互增强：

- KD-tree 中距离不超过 `1+1e-8` 的点对55928351个；
- 平方距离在 `4e-8` 窄带内的点对247392个；
- 除已有167927边外，新增79465条边全部通过32维齐次恒等式；
- 增强列边子图恰有247392边，CNF为145230变量、1556469子句；
- 数值窄带只用于寻找边，尚未配有区间排除，故不能据此声称边表完整；
- CaDiCaL195、Glucose4/DRAT、MapleChrono及带线性残余颜色对称破除的
  CaDiCaL153正在求解，当前均为 `UNKNOWN/RUNNING`。

残余颜色对称破除用29046个前缀辅助变量表达“颜色3先于颜色4首次
出现”，新增116183条短子句；它只去除固定三角形后剩余的颜色互换，
保持可满足性等价。MergeSat3运行352.34秒未决后被明确记录为
`UNKNOWN_INTERRUPTED`。

### 当前严格状态

- 下界 6：**未证明**；
- 完整 8329 点基础图：**精确完整且 SAT**；
- 10229 点完整边阻塞图：**精确完整且 SAT**；
- 最新无重复商图23046点、137927边：**已列明边精确且 SAT，尚未补齐所有单位边**；
- 没有不可 5 染色结论，也没有本项目目标所需的 DRAT/LRAT。

## 2026-08-10—15：增量边阶梯、闭包反例与全投影强制点对

### 29046 点交互图的 SAT 反例

原先直接求解停滞的 29046 点、247392 边图已用增量边阶梯解决。
每次加入当前模型已满足的全部新边和一条随机冲突边，在同一 SAT
会话中重求。种子 `20260811` 在 42.31 秒内到达全图 SAT，色类大小
`[3912,6348,6039,6326,6421]`；247392 条边全部独立逐边验证。

后续闭包和交互增广使用同一策略：

| 检查点 | 列边 | 新交互边 | 增量 SAT 时间 | 状态 |
|---:|---:|---:|---:|:---|
| 29546 | 252332 | 2440 | 31.90 s | SAT |
| 30046 | 257285 | 2453 | 10.09 s | SAT |
| 30983 | 266694 | 4724 | 32.27 s | SAT |
| 31966 | 276361 | 4752 | 33.85 s | SAT |

中间的投影单点闭包批次分别新增 500、500、937、946 个精确点；
所有保留坐标均为 32 维 D4 基上的有理齐次证书，所有保留边均用
齐次单位距离恒等式验证。交互边的浮点窄带仅用于候选预筛，
尚没有区间完备性证明；这不影响列边子图未来可能的 UNSAT 含意。

### 全投影四色强制点对

在 31966 点检查点上，不再只使用 8329 点基图作圆心，而是让全部
已认证的投影点参与四色强制点恢复。对当前 5-染色的缺失色
0—3 顺次扫描和施加：

| 缺失色 | 扫描前点数 | 精确强制点 | 精确单位点对 | 用到的候选 | 施加后点数 | 施加后边数 | SAT 时间 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 31966 | 2624 | 602 | 897 | 32863 | 280551 | 8.97 s |
| 1 | 32863 | 8012 | 1478 | 1914 | 34777 | 289685 | 9.51 s |
| 2 | 34777 | 9437 | 2758 | 3252 | 38029 | 305451 | 20.10 s |
| 3 | 38029 | 11479 | 4531 | 4914 | 42943 | 329638 | 7.44 s |

每个候选通过看到其余四色被迫取缺失色；每条内部单位边因而使
对的两端不可同时着色。施加器会重放四个生成邻居颜色、齐次坐标、
精确内部单位边和旧染色，然后写检查点再重求 SAT。

42943 点检查点的结果为
`PROJECTIVE_FORCED_PAIRS_WITH_5_COLORING`；整体工件写入耗时
675.92 秒，其中 SAT 仅 7.44 秒。缺失色 4 的全投影精确扫描随后完成：
62512400 对数值圆心候选产生 25883 个公共交点，排除已有点后有
14164 个，其中 13765 个通过四边精确恢复。候选间得到 6758 条
精确单位点对，涉及 6877 个候选；恢复用时 3185.37 秒，点对过滤
用时 1069.27 秒。扫描证书为
`artifacts/cegis_38029_projective_pairs_color3/projective-forced-color4-pairs.json`；
施加缺失色 4 的整批点对新增 6877 点、27508 条生成边和
6758 条内部点对边，得到 49820 点、363904 边检查点。CaDiCaL195
在 7.53 秒内找到合法 5-染色，色类为
`[7137,9643,10399,11160,11481]`；整批处理共 1726.55 秒。363904 条
列边已独立逐边验证。工件哈希为：

- 边文件：`9bc9b77677dcd748b5f75b88d0fd563b5b903f6bcd056dec63e48f8de8318c7a`；
- 图 JSON：`b18efce2faf628362344ff5995b447950b9b028fead7eff3570afd21b0a19984`；
- 5-染色 CNF：`ee038e64cb28610341ed5315629ef46ee258d837633318c311aeac0a9c556e42`。

### 49820 点交互边增广

旧的 `cKDTree.query_pairs` 会一次物化半径内的所有点对，在本图上
需要容纳约 1.48 亿对。改用分块 KD-tree 查询后得到：

- 半径内数值点对 148423243 个；
- 单位距离窄带内 462566 对；
- 在旧有 363904 条边外，新增 98662 条经 D4 齐次代数恒等式
  严格认证的单位边；
- 目标已列边子图共 462566 边。

数值窄带在这次扫描中恰好与严格通过的最终边数一致，但它仍
只是预筛，没有区间排除证书，所以不声称边集完备。

直接冷启动困难，故从 363904 边 SAT 检查点启动增量边阶梯。
以下运行都在某次 SAT 调用长时间无返回后人工中断，严格状态只是
`UNKNOWN_INTERRUPTED`：

| 种子 | 每轮已满足边上限 | 已完成 SAT 调用 | 停滞调用 |
|---:|---:|---:|---:|
| 20260815 | 无上限 | 6 | 7 |
| 20260816 | 无上限 | 1 | 2 |
| 20260817 | 无上限 | 0 | 1 |
| 20260818 | 5000 | 17 | 18 |
| 20260819 | 1000 | 42 | 43 |
| 20260820 | 250 | 252 | 253 |
| 20260821 | 50 | 966 | 967 |

种子 `20260822`、批次上限 10 的更细阶梯正在运行。阶梯只用精确已列
边的颜色约束；任何实际 UNSAT 返回仍必须另行导出并检查 DRAT/LRAT。

### 当前严格状态

- 当前最新已解图：49820 点、363904 条已认证列边，**SAT**；
- 49820 点、462566 边交互增广图：**SAT/UNSAT 尚未决**；
- 坐标：固定 32 维 D4 代数上的精确齐次有理系数；
- 图完备性：只对 8329 点基图已证明；大图是仅列出部分
  已认证单位边的精确子图；
- 下界 6：**未证明**；无 5-UNSAT，无 DRAT/LRAT。

移动工作目录后通过旧路径符号链接保留任务权限，然后在新物理
路径上运行全部回归测试：`64 passed in 754.62s`。
